{ "name": "bare-select"
, "version": "0.0.0-work-in-progress"
, "description": "A customizable replacement for <select>."

, "dependencies":
  { "as": "0.4.1"
  , "stereo": "0.4.0"
  }

, "scripts":
  { "build": "cd module && babel --out-dir ../ *.js"
  , "clean": "git reset && echo '/node_modules/' > .gitignore && git add .gitignore && git stash save --include-untracked --keep-index '`npm run clean` trash can' && git clean --force -d && git reset --hard && echo '\nclean: Uncommitted and ignored files have been moved to git’s stash. To restore them run `git stash pop --quiet; git checkout .gitignore`.'"
  , "coveralls": "npm run coverage && cat .es5/coverage/lcov.info | coveralls"
  , "coverage": "npm run test:build && cd .es5 && istanbul cover test.js"
  , "develop": "nodangel --ignore node_modules --exec 'npm run --silent test:lite'"
  , "prepublish": "npm run clean && npm run build"
  , "test": "jshint . && npm run test:build && node .es5/test.js | tap-spec"
  , "test:build": "babel *.js test/*.js test/*/*.js module/*.js --out-dir .es5"
  , "test:lite": "babel-node test.js | tap-spec"
  }

, "devDependencies":
  { "babel": "5.2.9"
  , "coveralls": "2.11.2"
  , "istanbul": "0.3.13"
  , "jshint": "2.7.0"
  , "nodangel": "1.3.8"
  , "tap-spec": "2.2.2"

  , "1-liners": "0.1.4"
  , "jsdom": "3.1.2"
  , "tape-catch": "1.0.4"
  , "virtual-dom": "2.0.1"
  }

, "files":
  [ "/*.js"
  , "/module/"
  , "/test/"
  , "/Readme.md"
  , "/License.md"
  ]

, "license": "MIT"
, "keywords":
  [ "…"
  ]
, "contributors":
  [ "Tomek Wiszniewski <t.wiszniewski@gmail.com>"
  ]
, "repository":
  { "type": "git"
  , "url": "git@github.com:studio-b12/bare-select.git"
  }
}
